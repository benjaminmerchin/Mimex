FROM node:22

RUN apt-get update \
  && apt-get install -y --no-install-recommends ffmpeg curl jq \
  && rm -rf /var/lib/apt/lists/*

RUN corepack enable

WORKDIR /app

CMD ["node", "server/dist/index.js"]
